<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icono_estudio_fondo_negro"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icono_estudio_fondo_negro"
        android:supportsRtl="true"
        android:theme="@style/Theme.Stock"
        tools:targetApi="31" >
        <activity
            android:name=".principal"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".primera" />
        <activity android:name=".info" />
        <activity android:name=".puestos" />
        <activity android:name=".contador" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>